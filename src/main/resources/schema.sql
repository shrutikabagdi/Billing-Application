CREATE TABLE IF NOT EXISTS customer (
                                        id INT PRIMARY KEY AUTO_INCREMENT,
                                        name VARCHAR(100),
    phone BIGINT,
    email VARCHAR(100),
    address VARCHAR(255)
    );

